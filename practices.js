//str.length
const str = "abcd";
const strLength = str.length;
console.log(strLength); //returns exact length (1-2-3-4! no zero or shit)
console.log(str.charAt(3)); //charAt start with 0
console.log(str.charAt(str.length - 1)); //last index
